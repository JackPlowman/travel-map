name: Clean Caches

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

permissions: {}

jobs:
  clean-caches:
    name: Clean Caches
    permissions:
      contents: read
    uses: JackPlowman/reusable-workflows/.github/workflows/common-clean-caches.yml@cc8b3c353be3aa159dbd5c71fb78ba84f50873ec # v2025.07.01.01
    secrets:
      workflow_github_token: ${{ secrets.GH_TOKEN }}
